<?php
/*
 * Trunk configuration defaults
 */
$config['trunk'] = [
    // the truncated string max length in characters
    'length' => 80,
    // add ellipses if the string is truncated?
    'ellipses' => false,
    // don't cut off words when truncating.
    // This will result in a possibly longer resulting string length
    'preserveWords' => false
];